# Exclude test fixtures and tests (not needed for package consumers)
test/

# Note: bin/ is published so the `libcompress` executable (pubspec executables:)
# is installable via `dart pub global activate libcompress`.

# Ignore internal documentation files and any AI kibble
AGENTS.md
GEMINI.md
CLAUDE.md
.claude
lib/src/**/*.md
lib/src/**/*.txt

# Ignore tool files
tool/
